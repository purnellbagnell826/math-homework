function getRandomInt(max) {
    return Math.floor(Math.random() * max);
}

// Example usage in your project
let randomNumber = getRandomInt(10); // Generate a random integer between 0 and 9
console.log(`Random number: ${randomNumber}`);
